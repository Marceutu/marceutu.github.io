/* VARIABLES */
:root {
    --azul-institucional: #1A365D;
    --gris-oxford: #4A5568;
    --blanco-fondo: #F7FAFC;
}

/* 1. RESET Y ESTRUCTURA BASE */
html, body {
    height: 100%;
    margin: 0;
    padding: 0;
    overflow-x: hidden; /* Evita que la página se mueva hacia los lados */
}

body {
    font-family: 'Montserrat', sans-serif;
    background-color: var(--blanco-fondo);
    display: flex;
    flex-direction: column;
}

.main-content {
    flex: 1;
}

/* 2. NAVBAR RESPONSIVA (MENÚ) */
.navbar {
    background-color: #ffffff;
    border-bottom: 3px solid var(--azul-institucional);
    padding: 10px 0;
    width: 100%;
}

.navbar ul {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column; /* 👈 EN MÓVIL: Una opción debajo de otra */
    align-items: center;    /* 👈 CENTRADO TOTAL */
    gap: 10px;
}

.navbar li {
    width: 100%;
    text-align: center;
}

.navbar a {
    text-decoration: none;
    color: var(--gris-oxford);
    font-weight: 600;
    text-transform: uppercase;
    font-size: 14px;
    display: block;
    padding: 8px 0;
}

/* Resaltar la página actual (opcional, si añades la clase .active) */
.navbar a:hover {
    color: var(--azul-institucional);
    background-color: #f1f5f9;
}

/* 3. CONTENEDOR Y TARJETAS */
.centrado {
    padding: 20px;
    max-width: 900px;
    margin: 0 auto;
}

.columnas {
    display: flex;
    flex-direction: column; /* 👈 TODO VERTICAL */
    gap: 25px;
}

.columna {
    background: white;
    padding: 20px;
    border-radius: 12px;
    box-shadow: 0 4px 15px rgba(0,0,0,0.08);
    border-left: 6px solid var(--azul-institucional);
    display: flex;
    flex-direction: column; /* 👈 FUERZA EL ORDEN VERTICAL */
    align-items: flex-start;
}

/* 4. BOTONES CORREGIDOS (DEBAJO DEL TEXTO) */
/* Esto arregla los botones de tus fotos que se salían de lado */
.btn-link, .btn-ejercicio, .btn-proyecto, .columna a {
    position: static !important; /* 👈 ANULA EL COSTADO */
    display: block !important;
    width: 100% !important;     /* 👈 OCUPA TODO EL ANCHO ABAJO */
    margin-top: 15px !important; /* 👈 SEPARA DEL TEXTO */
    background-color: var(--azul-institucional) !important;
    color: white !important;
    text-align: center;
    padding: 12px 0;
    border-radius: 6px;
    text-decoration: none;
    font-weight: 500;
    box-sizing: border-box;
}

/* 5. AJUSTES PARA PANTALLAS GRANDES (PC) */
@media (min-width: 768px) {
    .navbar ul {
        flex-direction: row; /* En PC vuelve a ser horizontal */
        justify-content: center;
        gap: 30px;
    }
    
    .navbar li {
        width: auto;
    }

    .columnas {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    }
    
    .btn-link, .btn-ejercicio, .btn-proyecto, .columna a {
        width: auto !important;
        padding: 10px 25px;
        align-self: flex-end; /* En PC los botones pueden ir a la esquina */
    }
}

/* FOOTER */
footer {
    background: var(--azul-institucional);
    color: white;
    text-align: center;
    padding: 25px;
    font-size: 13px;
    margin-top: 30px;
}
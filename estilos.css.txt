/* 1. RESET RADICAL PARA EVITAR ERRORES */
* {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
}

:root {
    --azul: #1A365D;
    --gris: #4A5568;
    --fondo: #F7FAFC;
}

body {
    font-family: 'Montserrat', sans-serif;
    background-color: var(--fondo);
    display: flex;
    flex-direction: column;
    min-height: 100vh;
}

/* 2. NAVBAR: SOLUCIÓN AL MENÚ CORTADO */
.navbar {
    background: #ffffff;
    border-bottom: 3px solid var(--azul);
    width: 100%;
    padding: 10px 0;
}

.navbar ul {
    list-style: none;
    display: flex;
    flex-direction: column; /* 👈 En móvil: uno debajo del otro */
    align-items: center;
    gap: 5px;
}

.navbar a {
    text-decoration: none;
    color: var(--gris);
    font-weight: 600;
    font-size: 13px;
    padding: 10px;
    display: block;
    text-transform: uppercase;
}

/* 3. TARJETAS: SOLUCIÓN AL BOTÓN CORRIDO */
.centrado {
    padding: 15px;
    max-width: 1000px;
    margin: 0 auto;
    width: 100%;
}

.columnas {
    display: flex;
    flex-direction: column; /* 👈 Todo vertical por defecto */
    gap: 20px;
}

.columna {
    background: white;
    padding: 20px;
    border-radius: 8px;
    box-shadow: 0 4px 10px rgba(0,0,0,0.1);
    border-left: 5px solid var(--azul);
    /* 👈 Esto es clave para que el botón no flote al costado */
    display: block !important; 
    position: relative !important;
}

/* 4. BOTONES: FORZAR DEBAJO DEL TEXTO */
/* Usamos !important para anular cualquier código viejo que los mueva */
.btn-link, .btn-ejercicio, .btn-proyecto, .columna a {
    position: static !important; /* 👈 Anula el 'absolute' que los saca de lugar */
    display: block !important;
    width: 100% !important;     /* 👈 Ocupa todo el ancho */
    margin-top: 20px !important; /* 👈 Se separa del texto hacia abajo */
    background: var(--azul) !important;
    color: white !important;
    text-align: center !important;
    padding: 12px !important;
    border-radius: 5px !important;
    text-decoration: none !important;
}

/* 5. AJUSTES PARA COMPUTADORA (PANTALLA GRANDE) */
@media (min-width: 768px) {
    .navbar ul {
        flex-direction: row; /* En PC vuelve a ser horizontal */
        justify-content: flex-end;
        padding-right: 50px;
    }

    .columnas {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    }
}

/* FOOTER */
footer {
    background: var(--azul);
    color: white;
    text-align: center;
    padding: 25px;
    margin-top: auto;
}